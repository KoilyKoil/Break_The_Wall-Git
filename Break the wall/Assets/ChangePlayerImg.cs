using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangePlayerImg : MonoBehaviour
{
    /*
    public class MyPlayer
    {
        public Animator ani;
    }

        public GameObject playerPrefab;

        GameObject go = Instantiate(playerPrefab);

        MyPlayer player = go.GetComponent<MyPlayer>();

        player.ani.runtimeAnimatorController=(RuntimeAnimatorController)RuntimeAnimatorController.Instantiate(Resource.Load("Anim\\JumpAnim", typeof(RuntimeAnimatorController));
    */
}
